<context path="." updated="2026-05-31">

Crossbot — plugin Hermes para mensagem bot-to-bot no Telegram.

## Summary

- Repo: hermes-crossbot-telegram (antes hermes-community-plugins)
- VERSION — release 0.5.0 (pré-release)
- README.md — índice + install via scripts/install.sh
- plugins/crossbot/ — plugin unificado (histórico + outbox + Kanban + mention relay)
- plugins/async-delegate/ — opcional
- scripts/install.sh — instalador (cross-bot | all | nome)
- docs/onboarding/ — guias deploy e workspace
- docs/reference/ — debug, feature map

## Constraints

- MUST expor register(ctx) em cada plugin
- MUST usar CROSSBOT_DB_PATH (ou alias MULTI_AGENT_TG_DB_PATH) global em multi-profile
- Workers Kanban MAY usar crossbot_cli.py como fallback

## Guidance

- SHOULD ./scripts/install.sh cross-bot para deploy
- SHOULD docs/onboarding/HANDOFF-DEPLOY.md para validação
- SHOULD docs/onboarding/05-fui-ao-mercado.md para benchmark

</context>
